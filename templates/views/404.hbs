<!DOCTYPE html>
<html>
    <head>
        <title>404 Error</title>
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        <h1>{{title}}</h1>
        <p>{{> header}}</p>
        <p>{{errorMessage}}</p>
        <h4>{{> footer}}</h4>
    </body>
</html>